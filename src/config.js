const MONGO_COLLECTION = {
  customers: 'customers'
};

const MONGO_URL = 'mongodb://mongo:27017/contact-list';

const MONGO_DB_NAME = 'customerContact';

const PORT = 3000;

module.exports = {
  MONGO_COLLECTION,
  MONGO_URL,
  MONGO_DB_NAME,
  PORT
};
